# midielectron

## 使い方

このアプリは、ライブ音声入力だけでなく、ローカルの音声ファイルや動画ファイルからもMIDIを書き出せます。

1. `npm install`
2. `npm run dev`
3. 画面右上の `Audio/Videoファイル` からファイルを選ぶ
4. 変換が終わったら保存ダイアログで `.mid` を出力する

ライブ入力を使う場合は、入力デバイスとMIDI出力ポートを選んで `Start` を押します。MIDI機器がなくても、ファイル変換だけで完結できます。